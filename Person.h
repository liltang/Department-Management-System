#include <string>
#include <iostream>

using namespace std;

class Person{

	private:
	//string name;
	//string bDay;
	int uid;
	//char gender;

	public:
	//Person(string name, string bDay, int uid, char gender);
	Person(int uid);
	int getUid(void);
  void print(void);



};
